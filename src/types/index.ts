export interface Doctor {
  name: string;
  title: string;
  image: string;
  bio: string;
}

export interface RelatedService {
  title: string;
  description: string;
  image: string;
  link: string;
}

